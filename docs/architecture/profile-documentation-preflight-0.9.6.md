# BPM 0.9.6 profile documentation preflight

Date: 2026-08-21

Backlog item: `BPM096-M10-06`

Status: completed. This record is source evidence only; it does not create a
documentation site, PDF, package, or installed artifact. M10-07 and M10-08
remain the owners of site and PDF work.

## Exact DITA source inventory

The M10 reader change set contains 31 English topics and their five authored
localized peers: 186 topic files in six locales. It also contains the six
locale `maps/keys.ditamap` files, for 192 changed DITA sources. For every row,
the English source is
`documentation/src/dita/en/<guide>/<topic>.dita`; the complete localized peer
set is the same `<guide>/<topic>.dita` under `ru`, `de`, `zh-CN`, `fr`, and
`es-ES`. This names every physical peer without treating a locale as an
English fallback.

| Guide | English topic IDs (each has all five named locale peers) | English / localized physical files |
| --- | --- | ---: |
| User | `ug-task-configure-browser-access-defaults`, `ug-task-configure-firefox-ai-policies`, `ug-task-configure-home-search-navigation`, `ug-task-configure-security-privacy`, `ug-task-configure-users-addons-sites`, `ug-task-create-first-profile`, `ug-task-duplicate-profile`, `ug-task-review-guided-profile`, `ug-task-save-export-guided-profile`, `ug-task-use-all-settings`, `ug-task-use-guided-editor`, `ug-task-use-json-editor`, `ug-troubleshoot-policy-validation`, `ug-troubleshoot-profile-name`, `ug-troubleshoot-schema-mismatch` | 15 / 75 |
| Firefox Policy | `fx-concept-complex-policy-families`, `fx-concept-policy-selection`, `fx-concept-release-esr-differences`, `fx-concept-starter-presets`, `fx-task-review-complex-policy-configuration` | 5 / 25 |
| CIS Settings | `cis-concept-manual-review-exceptions`, `cis-concept-presets-layers-merge`, `cis-task-run-level-1-workflow`, `cis-task-run-level-2-hardened-workflow`, `cis-task-select-cis-baseline`, `cis-task-trace-cis-source`, `cis-task-verify-cis-deviation` | 7 / 35 |
| Administrator | `admin-concept-api-conventions`, `admin-concept-api-limitations`, `admin-task-sync-profile-lifecycle`, `admin-task-use-reusable-api-examples` | 4 / 20 |

The six locale key maps are exactly
`documentation/src/dita/{en,ru,de,zh-CN,fr,es-ES}/maps/keys.ditamap`.

## Other changed source inputs

- Help/search: `documentation/config/search-normalization-aliases-0.9.0.json`,
  `documentation/config/all-settings-help-target-map-0.9.1.json`,
  `app/documentation/manifest.py`, and
  `app/documentation/site/ui-target-map.json`.
- Screenshot disposition: `documentation/config/user-guide-screenshot-matrix-0.9.1.json`,
  `documentation/config/user-guide-screenshot-visual-qa-0.9.1.json`, and 30
  source PNGs: five scenarios (`profile-preparation-create`,
  `profile-preparation-duplicate`, `guided-urls-sites-navigation`,
  `guided-certificates-trust`, `guided-extensions`) in each of `en`, `ru`,
  `de`, `zh-CN`, `fr`, and `es-ES`.
- Source evidence: the M10-01 impact inventory, M10-03 locale terminology
  audit, and M10-05 editorial review. Their claims are rechecked by the
  preflight gates below rather than copied into generated output.

## Required gate record

The following results are filled only after the corresponding owner command
finishes. A site or PDF build is intentionally out of scope for this task.

| Gate | Result |
| --- | --- |
| `make docs-fast-check DOCS_CHANGED='…exact M10 source set…'` | passed — final exact-set rerun, 296 inputs |
| `make test-docs-contract` | passed — final rerun, 1002 tests in 37.68 s |
| `make docs-validate` | passed — DITA, metadata, source links, and generated links valid in all six locales |
| `make check-locale-catalogs` | passed |
| `make test-locale-contract` | passed — 78 tests in 110.42 s |
| `make test-firefox-schema-contract` | passed — 79 tests in 86.16 s |
| `make verify-firefox-schema-matrix` | passed — four channels, offline reproducibility and channel proof |
| `make verify-firefox-conversion-matrix` | passed — all 12 ordered source/target pairs |
| OpenAPI/API, Firefox/CIS, and compliance contract groups | passed — 11 API tests and 55 Firefox/CIS/compliance tests |
| `make frontend-profile-graph` | passed — 42 owned JS modules and 9 CSS layers |
| `make docs-snapshot` | passed — regenerated by owner before final contract confirmation |
| `make codex-snapshot` | passed — final owner snapshot |

## Failure policy

Repair the owning authored source, contract, or generated-owner command input;
never hand-edit generated site, search, manifest, PDF, package, screenshot
report, or snapshot output. Any failure remains recorded here with its
classification, repair, and focused rerun.

## Repairs and reruns

| Gate / classification | Source-level repair | Focused result |
| --- | --- | --- |
| `docs-fast-check`: 80 localized DITA structural/parity findings | Completed the matching authored locale sections and corrected their structural ownership; no fallback topic or generated DITA was edited. | Exact-set rerun passed for 296 inputs. |
| `test-docs-contract`: stale API/topic, UI-markup, localized literal, and snapshot contracts | Repaired the owning DITA, documentation-contract, and semantic-markup sources. The one stale documentation snapshot was regenerated only with its owner command. | Full rerun passed: 1002 tests. |
| `test-locale-contract`: 13 failing checks | Localized the authored extension/certificate strings in `app/i18n_src`, corrected native Chinese terminology, and made the visible-English guard recognize actual technical identifiers and Windows paths without weakening ordinary prose checks. Runtime catalogs were regenerated by their owner. | Full rerun passed: 78 tests. |
| Firefox schema matrix: specialized certificate placement omitted from aggregate proof | Made the matrix verifier count the full dedicated certificate component while preserving the narrower four-control posture card. A package-reexport import was corrected to use the owning `catalog` module. | Retry passed for all four channels. |
| Firefox documentation inventory: generated owner artifact stale | Kept the existing 0.9.6 source declaration and ran `build_firefox_policy_documentation_inventory.py`; no JSON or Markdown inventory was hand edited. | Firefox/CIS/compliance retry passed: 55 tests. |
| Shell `pytest` lookup | The project runner is `.venv/bin/pytest`; invoking it directly avoids an absent shell-path executable and does not alter the virtual environment. | All recorded pytest groups used the project runner. |
