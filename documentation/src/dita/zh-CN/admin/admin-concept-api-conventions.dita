<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE concept PUBLIC "-//OASIS//DTD DITA Concept//EN" "concept.dtd">
<concept id="admin-concept-api-conventions" xml:lang="zh-CN" audience="administrator devops integrator security-reviewer" product="bpm-0-9-0" platform="web">
  <title>API conventions</title>
  <shortdesc>在阅读 BPM 0.9.0 OpenAPI contracts 和构建 integration requests 时使用这些 conventions。</shortdesc>
  <conbody>
    <section id="a-base-url"><title>Base URLs and discovery</title><p>示例使用 <codeph>$BPM_BASE_URL</codeph> 作为 deployment placeholder。当前 contract 从 <codeph>/openapi.json</codeph> 获取；仅用于 interactive inspection 时可使用 FastAPI schema UIs <codeph>/docs</codeph> 和 <codeph>/redoc</codeph>。未来 product documentation portal 保留给 <codeph>/help/</codeph>，因此不要把 <codeph>/docs</codeph> 视为 DITA documentation route。</p></section>
    <section id="a-content-types"><title>Content types</title><p>大多数 programmatic operations 使用 JSON request 和 response bodies。Firefox import 接受 <codeph>application/json</codeph> 和 <codeph>multipart/form-data</codeph>；unsupported media types 可能返回 <codeph>415</codeph>。Firefox export 返回 <codeph>application/json</codeph>，请求 download output 时可添加 <codeph>Content-Disposition</codeph>。</p></section>
    <section id="a-identifiers"><title>Identifiers and lifecycle</title><p>Profile IDs 是 BPM 拥有的 integer path identifiers。create、import、read 和 update operations 后应保存返回的 <codeph>id</codeph> 和 <codeph>revision</codeph>。Profile names 唯一，不能通过 <codeph>ProfileUpdate</codeph> 修改。Lifecycle values 为 active、archived 和 all；<codeph>include_deleted</codeph> 是 compatibility flag，必须与 lifecycle 一起解释。</p></section>
    <section id="a-schemas"><title>Models and schema channels</title><p><codeph>ProfileCreate</codeph>、<codeph>ProfileUpdate</codeph> 和 <codeph>ProfileRead</codeph> 是 BPM normalized profile shapes。Firefox import/export 使用带 top-level <codeph>policies</codeph> 的 canonical <codeph>policies.json</codeph>。Validation 使用 <codeph>ValidationRequest</codeph> 和 supported schema channels，例如 Firefox ESR 和 Release；plain policy mapping 仍是 compatibility behavior，不是 preferred external shape。</p></section>
    <section id="a-query"><title>Filtering, sorting, and pagination</title><p>Profile list 和 stats operations 支持 search、schema、validation state、lifecycle 和 include-deleted filters。List pagination 使用 offset/limit，default <codeph>limit=50</codeph>，maximum <codeph>200</codeph>，<codeph>offset=0</codeph>。Sorting 使用当前描述字段，例如 <codeph>created_at</codeph>、<codeph>updated_at</codeph>、<codeph>name</codeph>、<codeph>schema_version</codeph> 和 <codeph>id</codeph>，order 为 <codeph>asc</codeph> 或 <codeph>desc</codeph>。</p></section>
    <section id="a-errors"><title>Status and error handling</title><p>请同时使用 status codes 和 body shape。<codeph>200</codeph>、<codeph>201</codeph> 和 <codeph>204</codeph> 是 success families，但 validation 可返回 <codeph>200</codeph> 且 <codeph>ok=false</codeph>。Error bodies 使用 FastAPI <codeph>detail</codeph>，可能包含 string、validation list，或带 <codeph>message</codeph>、<codeph>error</codeph>、<codeph>issues</codeph> 的 BPM object。</p></section>
  </conbody>
  <related-links>
    <link keyref="topic.admin-concept-integration-audience"/>
    <link keyref="topic.admin-concept-supported-integration-patterns"/>
    <link keyref="topic.ug-task-import-policies-json"/>
    <link keyref="topic.ug-task-export-policies-json"/>
  </related-links>
</concept>
